/** @type {import('prettier').RequiredOptions} */
const config = {
  arrowParens: 'always',
  printWidth: 120,
  proseWrap: 'preserve',
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'es5',
  useTabs: false,
}

module.exports = config
